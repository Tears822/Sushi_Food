@page "/contact"
@using HidaSushi.Client.Services
@inject NavigationManager Navigation
@inject ILocalizationService LocalizationService
@inherits FlowbitePage
@implements IDisposable

<PageTitle>@LocalizationService.GetString("Contact.Title") - HIDA SUSHI</PageTitle>

<!-- Debug Info (remove this later) -->
<div class="bg-yellow-100 border border-yellow-400 text-yellow-700 px-4 py-3 rounded mb-4">
    <strong>Debug Info:</strong> Current Language: @LocalizationService.CurrentLanguage | 
    Title: @LocalizationService.GetString("Contact.Title") | 
    ContactUs: @LocalizationService.GetString("Contact.ContactUs")
</div>

<!-- Hero Section -->
<section class="relative bg-gradient-to-br from-purple-600 via-pink-600 to-amber-600 text-white py-16 lg:py-24 overflow-hidden">
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 font-poppins">
            @LocalizationService.GetString("Contact.ContactUs")
        </h1>
        <p class="text-xl md:text-2xl text-purple-100 mb-8 max-w-3xl mx-auto leading-relaxed">
            @LocalizationService.GetString("Contact.ContactDescription")
        </p>
    </div>
    
    <!-- Floating Elements -->
    <div class="absolute top-20 left-10 text-5xl opacity-20 animate-bounce">📞</div>
    <div class="absolute bottom-20 right-10 text-4xl opacity-20 animate-pulse">📍</div>
    <div class="absolute top-1/3 right-1/4 text-3xl opacity-20">⏰</div>
</section>

<!-- Contact Information Section -->
<section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("Contact.GetInTouch")
            </h2>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
            
            <!-- Phone -->
            <div class="text-center p-6 bg-gradient-to-br from-purple-50 to-pink-50 rounded-2xl border border-purple-100">
                <div class="text-4xl mb-4">📞</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.Phone")</h3>
                <p class="text-gray-600 mb-2">+33 1 23 45 67 89</p>
                <p class="text-sm text-gray-500">@LocalizationService.GetString("Contact.PhoneAvailable")</p>
            </div>
            
            <!-- Email -->
            <div class="text-center p-6 bg-gradient-to-br from-pink-50 to-amber-50 rounded-2xl border border-pink-100">
                <div class="text-4xl mb-4">✉️</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.Email")</h3>
                <p class="text-gray-600 mb-2">contact@hidasushi.fr</p>
                <p class="text-sm text-gray-500">@LocalizationService.GetString("Contact.EmailResponse")</p>
            </div>
            
            <!-- Location -->
            <div class="text-center p-6 bg-gradient-to-br from-amber-50 to-orange-50 rounded-2xl border border-amber-100">
                <div class="text-4xl mb-4">📍</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.Location")</h3>
                <p class="text-gray-600 mb-2">123 Rue de la Gastronomie</p>
                <p class="text-sm text-gray-500">@LocalizationService.GetString("Contact.LocationDescription")</p>
            </div>
        </div>
        
        <!-- Social Media -->
        <div class="text-center mb-16">
            <h3 class="text-2xl font-bold text-gray-800 mb-4">@LocalizationService.GetString("Contact.FollowUs")</h3>
            <p class="text-gray-600 mb-6">@LocalizationService.GetString("Contact.FollowUsDescription")</p>
            <div class="flex justify-center space-x-6">
                <a href="#" class="text-3xl hover:scale-110 transition-transform duration-200">📘</a>
                <a href="#" class="text-3xl hover:scale-110 transition-transform duration-200">📷</a>
                <a href="#" class="text-3xl hover:scale-110 transition-transform duration-200">🐦</a>
            </div>
        </div>
    </div>
</section>

<!-- Business Hours Section -->
<section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("Contact.BusinessHours")
            </h2>
        </div>
        
        <div class="max-w-2xl mx-auto">
            <div class="bg-white rounded-2xl shadow-lg p-8">
                <div class="space-y-4">
                    <div class="flex justify-between items-center py-3 border-b border-gray-100">
                        <span class="font-medium text-gray-700">@LocalizationService.GetString("Contact.MondayThursday")</span>
                        <span class="text-gray-600">11:00 - 22:00</span>
                    </div>
                    <div class="flex justify-between items-center py-3 border-b border-gray-100">
                        <span class="font-medium text-gray-700">@LocalizationService.GetString("Contact.FridaySaturday")</span>
                        <span class="text-gray-600">11:00 - 23:00</span>
                    </div>
                    <div class="flex justify-between items-center py-3">
                        <span class="font-medium text-gray-700">@LocalizationService.GetString("Contact.Sunday")</span>
                        <span class="text-gray-600">12:00 - 21:00</span>
                    </div>
                </div>
                
                <div class="mt-6 p-4 bg-gradient-to-r from-green-50 to-emerald-50 rounded-xl border border-green-200">
                    <div class="flex items-center justify-center space-x-2">
                        <div class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></div>
                        <span class="text-green-700 font-medium">@LocalizationService.GetString("Contact.CurrentlyOpen")</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Call to Action Section -->
<section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
            @LocalizationService.GetString("Contact.ReadyToOrder")
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
            <a href="/menu" class="group p-6 bg-gradient-to-br from-purple-50 to-pink-50 rounded-2xl border border-purple-200 hover:shadow-lg transition-all duration-300 hover:scale-105">
                <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🍣</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.BrowseMenu")</h3>
            </a>
            
            <a href="/build-roll" class="group p-6 bg-gradient-to-br from-pink-50 to-amber-50 rounded-2xl border border-pink-200 hover:shadow-lg transition-all duration-300 hover:scale-105">
                <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🎨</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.BuildCustomRoll")</h3>
            </a>
            
            <a href="/track-order" class="group p-6 bg-gradient-to-br from-amber-50 to-orange-50 rounded-2xl border border-amber-200 hover:shadow-lg transition-all duration-300 hover:scale-105">
                <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">📦</div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("Contact.TrackExistingOrder")</h3>
            </a>
        </div>
    </div>
</section>

<!-- Delivery Information Section -->
<section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("Contact.DeliveryInformation")
            </h2>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
            <div class="bg-white rounded-2xl p-8 shadow-lg">
                <div class="text-center">
                    <div class="text-4xl mb-4">🚚</div>
                    <h3 class="text-xl font-bold text-gray-800 mb-4">@LocalizationService.GetString("Contact.DeliveryFee")</h3>
                    <p class="text-gray-600 mb-4">@LocalizationService.GetString("Contact.FreeDelivery")</p>
                    <p class="text-sm text-gray-500">@LocalizationService.GetString("Contact.DeliveryTime")</p>
                </div>
            </div>
            
            <div class="bg-white rounded-2xl p-8 shadow-lg">
                <div class="text-center">
                    <div class="text-4xl mb-4">🏪</div>
                    <h3 class="text-xl font-bold text-gray-800 mb-4">@LocalizationService.GetString("Contact.PickupAvailable")</h3>
                    <p class="text-gray-600">@LocalizationService.GetString("Contact.PickupAvailable")</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- FAQ Section -->
<section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("Contact.FrequentlyAskedQuestions")
            </h2>
        </div>
        
        <div class="max-w-4xl mx-auto space-y-6">
            <div class="bg-gray-50 rounded-2xl p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-3">@LocalizationService.GetString("Contact.FAQFreshIngredients")</h3>
                <p class="text-gray-600">@LocalizationService.GetString("Contact.FAQFreshIngredientsAnswer")</p>
            </div>
            
            <div class="bg-gray-50 rounded-2xl p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-3">@LocalizationService.GetString("Contact.FAQDietaryRestrictions")</h3>
                <p class="text-gray-600">@LocalizationService.GetString("Contact.FAQDietaryRestrictionsAnswer")</p>
            </div>
            
            <div class="bg-gray-50 rounded-2xl p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-3">@LocalizationService.GetString("Contact.FAQPaymentMethods")</h3>
                <p class="text-gray-600">@LocalizationService.GetString("Contact.FAQPaymentMethodsAnswer")</p>
            </div>
            
            <div class="bg-gray-50 rounded-2xl p-6">
                <h3 class="text-lg font-bold text-gray-800 mb-3">@LocalizationService.GetString("Contact.FAQOrderTracking")</h3>
                <p class="text-gray-600">@LocalizationService.GetString("Contact.FAQOrderTrackingAnswer")</p>
            </div>
        </div>
    </div>
</section> 

@code {
    protected override void OnInitialized()
    {
        LocalizationService.OnLanguageChanged += OnLanguageChanged;
    }

    private void OnLanguageChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        LocalizationService.OnLanguageChanged -= OnLanguageChanged;
    }
} 