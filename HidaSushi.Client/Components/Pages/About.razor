@page "/about"
@using HidaSushi.Client.Services
@inject NavigationManager Navigation
@inject ILocalizationService LocalizationService
@inherits FlowbitePage

<PageTitle>@LocalizationService.GetString("About.Title") - HIDA SUSHI</PageTitle>

<!-- Hero Section -->
<section class="relative bg-gradient-to-br from-blue-600 via-purple-600 to-pink-600 text-white py-16 lg:py-24 overflow-hidden">
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 font-poppins">
            @LocalizationService.GetString("About.Title")
        </h1>
        <p class="text-xl md:text-2xl text-blue-100 mb-8 max-w-3xl mx-auto leading-relaxed">
            @LocalizationService.GetString("About.Subtitle")
        </p>
    </div>
    
    <!-- Floating Elements -->
    <div class="absolute top-20 left-10 text-5xl opacity-20 animate-bounce">🍣</div>
    <div class="absolute bottom-20 right-10 text-4xl opacity-20 animate-pulse">🎨</div>
    <div class="absolute top-1/3 right-1/4 text-3xl opacity-20">🌟</div>
</section>

<!-- Story Section -->
<section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            
            <!-- Story Content -->
            <div>
                <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                    @LocalizationService.GetString("About.StoryTitle")
                </h2>
                <p class="text-lg text-gray-700 leading-relaxed mb-6">
                    @LocalizationService.GetString("About.StoryDescription")
                </p>
                <p class="text-lg text-gray-700 leading-relaxed mb-6">
                    @LocalizationService.GetString("About.StoryDescription2")
                </p>
                <p class="text-lg text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.StoryDescription3")
                </p>
            </div>
            
            <!-- Story Image -->
            <div class="relative">
                <div class="bg-gradient-to-br from-purple-100 to-pink-100 rounded-3xl p-8">
                    <div class="text-center">
                        <div class="text-8xl mb-4">🍣</div>
                        <h3 class="text-2xl font-bold text-gray-800 mb-2">@LocalizationService.GetString("About.CulinaryPhilosophy")</h3>
                        <p class="text-gray-600 italic">"@LocalizationService.GetString("About.CulinaryPhilosophyQuote")"</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Stats Section -->
<section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            
            <!-- Signature Rolls Count -->
            <div class="text-center">
                <div class="text-4xl font-bold text-purple-600 mb-2">50+</div>
                <div class="text-lg text-gray-700">@LocalizationService.GetString("About.SignatureRollsCount")</div>
            </div>
            
            <!-- Years Experience -->
            <div class="text-center">
                <div class="text-4xl font-bold text-pink-600 mb-2">15+</div>
                <div class="text-lg text-gray-700">@LocalizationService.GetString("About.YearsExperience")</div>
            </div>
            
            <!-- Fresh Ingredients Count -->
            <div class="text-center">
                <div class="text-4xl font-bold text-blue-600 mb-2">100%</div>
                <div class="text-lg text-gray-700">@LocalizationService.GetString("About.FreshIngredientsCount")</div>
            </div>
        </div>
    </div>
</section>

<!-- Journey Section -->
<section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("About.TheJourney")
            </h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                @LocalizationService.GetString("About.JourneyDescription")
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            
            <!-- Tokyo Training -->
            <div class="bg-gradient-to-br from-purple-50 to-pink-50 rounded-2xl p-8 border border-purple-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">🇯🇵</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.TokyoTraining")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.TokyoTrainingDescription")
                </p>
            </div>
            
            <!-- Global Inspiration -->
            <div class="bg-gradient-to-br from-blue-50 to-purple-50 rounded-2xl p-8 border border-blue-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">🌍</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.GlobalInspiration")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.GlobalInspirationDescription")
                </p>
            </div>
            
            <!-- HIDA SUSHI Born -->
            <div class="bg-gradient-to-br from-green-50 to-blue-50 rounded-2xl p-8 border border-green-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">🏛️</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.HidaSushiBorn")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.HidaSushiBornDescription")
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Values Section -->
<section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                @LocalizationService.GetString("About.OurValues")
            </h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                @LocalizationService.GetString("About.OurValuesDescription")
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            
            <!-- Fresh Ingredients -->
            <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">🥬</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.FreshIngredients")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.FreshIngredientsDescription")
                </p>
            </div>
            
            <!-- Artisan Craftsmanship -->
            <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">🎨</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.ArtisanCraftsmanship")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.ArtisanCraftsmanshipDescription")
                </p>
            </div>
            
            <!-- Customer Experience -->
            <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-200">
                <div class="text-center mb-6">
                    <div class="text-4xl mb-4">💝</div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("About.CustomerExperience")</h3>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("About.CustomerExperienceDescription")
                </p>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-16 bg-gradient-to-r from-purple-600 to-pink-600">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
            @LocalizationService.GetString("About.ReadyToOrder")
        </h2>
        <p class="text-xl text-purple-100 mb-8">
            @LocalizationService.GetString("About.OrderQuestion")
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="/menu" class="inline-block bg-white text-purple-600 font-bold py-4 px-8 rounded-2xl hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-lg">
                @LocalizationService.GetString("About.ViewMenu")
            </a>
            <a href="/build-roll" class="inline-block bg-transparent text-white font-bold py-4 px-8 rounded-2xl border-2 border-white hover:bg-white hover:text-purple-600 transition-all duration-300 transform hover:scale-105">
                @LocalizationService.GetString("About.BuildYourOwn")
            </a>
        </div>
    </div>
</section>

@code {
    protected override void OnInitialized()
    {
        // Subscribe to language changes to refresh the component
        LocalizationService.OnLanguageChanged += OnLanguageChanged;
    }

    private void OnLanguageChanged()
    {
        // Force re-render when language changes
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        // Unsubscribe from language changes
        LocalizationService.OnLanguageChanged -= OnLanguageChanged;
    }
} 