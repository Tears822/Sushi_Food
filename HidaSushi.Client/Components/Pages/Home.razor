@page "/"
@using HidaSushi.Shared.Models
@using HidaSushi.Client.Services
@inject NavigationManager Navigation
@inject ICartService CartService
@inject IToastService ToastService
@inject ILogger<Home> Logger
@inject IApiService ApiService
@inject ILocalizationService LocalizationService
@inherits FlowbitePage

<PageTitle>HIDA SUSHI - @LocalizationService.GetString("Home.Title")</PageTitle>

<!-- Modern Hero Section with Sushi Pattern -->
<section class="relative min-h-screen bg-gradient-to-br from-slate-50 via-white to-green-50 overflow-hidden pattern-overlay-sushi">
    <!-- Floating Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-20 h-20 bg-gradient-to-br from-green-400/20 to-blue-400/20 rounded-full blur-xl animate-float"></div>
        <div class="absolute top-40 right-20 w-32 h-32 bg-gradient-to-br from-purple-400/20 to-pink-400/20 rounded-full blur-xl animate-float-delayed"></div>
        <div class="absolute bottom-20 left-1/4 w-24 h-24 bg-gradient-to-br from-orange-400/20 to-red-400/20 rounded-full blur-xl animate-float-slow"></div>
    </div>
    
    <!-- Animated Sushi Pattern Background -->
    <div class="absolute inset-0 bg-sushi-animated pointer-events-none"></div>
    
    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-20 pb-16">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center min-h-[80vh]">
            <!-- Left Content -->
            <div class="text-center lg:text-left animate-fade-in-up">
                <div class="mb-6">
                    <span class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-green-100 to-blue-100 text-green-800 text-sm font-medium rounded-full border border-green-200 shadow-sm">
                        <span class="w-2 h-2 bg-green-500 rounded-full mr-2 animate-pulse"></span>
                        @LocalizationService.GetString("Home.Subtitle")
                    </span>
                </div>
                
                <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 mb-6 leading-tight">
                    Premium
                    <span class="bg-gradient-to-r from-green-600 via-blue-600 to-purple-600 bg-clip-text text-transparent">
                        Sushi
                    </span>
                    <br />Delivered
            </h1>
                
                <p class="text-xl text-gray-600 mb-8 max-w-2xl leading-relaxed">
                    @LocalizationService.GetString("Home.Description")
            </p>
                
                <div class="flex flex-col sm:flex-row gap-4 mb-8">
                    <button @onclick="@(() => Navigation.NavigateTo("/menu"))" 
                            class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold rounded-2xl shadow-xl hover:shadow-2xl transform hover:scale-105 transition-all duration-300">
                        <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                        </svg>
                        @LocalizationService.GetString("Home.OrderNow")
                    </button>
                    
                    <button @onclick="@(() => Navigation.NavigateTo("/menu"))" 
                            class="inline-flex items-center px-8 py-4 bg-white hover:bg-gray-50 text-gray-900 font-semibold rounded-2xl shadow-lg hover:shadow-xl border border-gray-200 transform hover:scale-105 transition-all duration-300">
                        <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                        </svg>
                        @LocalizationService.GetString("Home.ExploreMenu")
                    </button>
                </div>
                
                <!-- Trust Indicators -->
                <div class="flex flex-wrap items-center gap-6 text-sm text-gray-500">
                    <div class="flex items-center">
                        <span class="text-yellow-500 mr-1">⭐</span>
                        <span class="font-medium text-gray-900">4.8</span>
                        <span class="ml-1">(2,847 reviews)</span>
                    </div>
                    <div class="flex items-center">
                        <span class="text-green-500 mr-1">🚚</span>
                        <span class="font-medium">25-35 min delivery</span>
                    </div>
                    <div class="flex items-center">
                        <span class="text-blue-500 mr-1">💳</span>
                        <span class="font-medium">Secure payment</span>
                    </div>
                </div>
            </div>
            
            <!-- Right Hero Image -->
            <div class="relative animate-fade-in-up lg:order-last">
                <div class="relative rounded-3xl overflow-hidden shadow-2xl transform rotate-3 hover:rotate-0 transition-transform duration-700">
                    <div class="aspect-[4/5] bg-gradient-to-br from-green-100 to-blue-100 flex items-center justify-center">
                        <div class="text-center">
                            <div class="text-8xl mb-4">🍣</div>
                            <p class="text-2xl font-bold text-gray-700">@LocalizationService.GetString("Home.PremiumSushi")</p>
                            <p class="text-lg text-gray-600">@LocalizationService.GetString("Home.FreshDelicious")</p>
                        </div>
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
                </div>
                
                <!-- Decorative elements -->
                <div class="absolute -top-4 -right-4 w-24 h-24 bg-gradient-to-br from-yellow-400 to-orange-400 rounded-full opacity-20 animate-pulse"></div>
                <div class="absolute -bottom-4 -left-4 w-16 h-16 bg-gradient-to-br from-blue-400 to-purple-400 rounded-full opacity-20 animate-bounce"></div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
                @LocalizationService.GetString("Home.WhyChooseTitle")
            </h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                @LocalizationService.GetString("Home.WhyChooseDescription")
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            <div class="group text-center p-8 rounded-3xl bg-gradient-to-br from-green-50 to-blue-50 hover:from-green-100 hover:to-blue-100 transition-all duration-300 transform hover:scale-105 border border-green-100">
                <div class="w-16 h-16 mx-auto mb-6 bg-gradient-to-br from-green-500 to-blue-500 rounded-2xl flex items-center justify-center text-white text-2xl group-hover:scale-110 transition-transform">
                    🍣
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">@LocalizationService.GetString("Home.FreshIngredientsTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.FreshIngredientsDescription")</p>
            </div>
            
            <div class="group text-center p-8 rounded-3xl bg-gradient-to-br from-purple-50 to-pink-50 hover:from-purple-100 hover:to-pink-100 transition-all duration-300 transform hover:scale-105 border border-purple-100">
                <div class="w-16 h-16 mx-auto mb-6 bg-gradient-to-br from-purple-500 to-pink-500 rounded-2xl flex items-center justify-center text-white text-2xl group-hover:scale-110 transition-transform">
                    👨‍🍳
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">@LocalizationService.GetString("Home.MasterChefTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.MasterChefDescription")</p>
            </div>
            
            <div class="group text-center p-8 rounded-3xl bg-gradient-to-br from-orange-50 to-red-50 hover:from-orange-100 hover:to-red-100 transition-all duration-300 transform hover:scale-105 border border-orange-100">
                <div class="w-16 h-16 mx-auto mb-6 bg-gradient-to-br from-orange-500 to-red-500 rounded-2xl flex items-center justify-center text-white text-2xl group-hover:scale-110 transition-transform">
                    🚚
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">@LocalizationService.GetString("Home.FastDeliveryTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.FastDeliveryDescription")</p>
            </div>
            
            <div class="group text-center p-8 rounded-3xl bg-gradient-to-br from-yellow-50 to-amber-50 hover:from-yellow-100 hover:to-amber-100 transition-all duration-300 transform hover:scale-105 border border-yellow-100">
                <div class="w-16 h-16 mx-auto mb-6 bg-gradient-to-br from-yellow-500 to-amber-500 rounded-2xl flex items-center justify-center text-white text-2xl group-hover:scale-110 transition-transform">
                    🎨
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">@LocalizationService.GetString("Home.CustomRollsTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.CustomRollsDescription")</p>
            </div>
        </div>
    </div>
</section>

<!-- Featured Rolls Section with Wave Pattern -->
<section class="py-16 bg-wave-layered pattern-overlay-wave">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                @LocalizationService.GetString("Home.SignatureRollsTitle")
            </h2>
            <p class="text-xl text-gray-600 leading-relaxed max-w-3xl mx-auto">
                @LocalizationService.GetString("Home.SignatureRollsDescription")
            </p>
        </div>
        
        @if (isLoadingRolls)
        {
            <div class="flex justify-center items-center py-20">
                <div class="text-center">
                    <div class="animate-spin rounded-full h-16 w-16 border-b-4 border-green-600 mx-auto mb-4"></div>
                    <p class="text-gray-600 text-lg">@LocalizationService.GetString("Home.LoadingSignatureRolls")</p>
                </div>
            </div>
        }
        else if (signatureRolls?.Any() == true)
        {
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                @foreach (var roll in signatureRolls.Take(6))
                {
                    <ProductCard Roll="roll" OnAddToCart="AddToCart" />
                }
            </div>
        }
        else
        {
            <div class="text-center py-20">
                <div class="text-6xl mb-4">🍱</div>
                <h3 class="text-2xl font-bold text-gray-900 mb-2">@LocalizationService.GetString("Home.ComingSoonTitle")</h3>
                <p class="text-gray-600 mb-6">@LocalizationService.GetString("Home.ComingSoonDescription")</p>
                <button @onclick="@(() => Navigation.NavigateTo("/menu"))" 
                        class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-2xl transition-all duration-300 transform hover:scale-105 shadow-lg">
                    @LocalizationService.GetString("Home.ViewFullMenu")
                </button>
            </div>
        }
    </div>
</section>

<!-- About Chef Section with Bamboo Pattern -->
<section class="py-20 bg-bamboo-layered pattern-overlay-bamboo">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            <div class="relative">
                <div class="bg-white/80 backdrop-blur-lg rounded-3xl p-8 shadow-xl border border-gray-200/50">
                    <div class="text-center">
                        <div class="text-6xl mb-6">👨‍🍳</div>
                        <h3 class="text-3xl font-bold text-gray-900 mb-4">Chef Jonathan</h3>
                        <p class="text-lg text-gray-700 leading-relaxed mb-6">
                            @LocalizationService.GetString("Home.AboutChefDescription")
                        </p>
                        <button @onclick="@(() => Navigation.NavigateTo("/about"))" 
                                class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-2xl transition-all duration-300 transform hover:scale-105 shadow-lg">
                            Learn More
                        </button>
                    </div>
                </div>
            </div>
            
            <div class="space-y-6">
                <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                    @LocalizationService.GetString("Home.AboutChefTitle")
                </h2>
                
                <p class="text-xl text-gray-700 leading-relaxed">
                    @LocalizationService.GetString("Home.AboutChefDescription")
                </p>
                
                <div class="grid grid-cols-2 gap-6">
                    <div class="text-center p-6 bg-white/60 backdrop-blur-lg rounded-2xl">
                        <div class="text-3xl font-bold text-green-600 mb-2">15+</div>
                        <div class="text-gray-700 font-medium">@LocalizationService.GetString("Home.SignatureRollsCount")</div>
                    </div>
                    <div class="text-center p-6 bg-white/60 backdrop-blur-lg rounded-2xl">
                        <div class="text-3xl font-bold text-blue-600 mb-2">100%</div>
                        <div class="text-gray-700 font-medium">@LocalizationService.GetString("Home.FreshIngredientsCount")</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- How It Works Section -->
<section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
                @LocalizationService.GetString("Home.HowItWorksTitle")
            </h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                @LocalizationService.GetString("Home.HowItWorksDescription")
            </p>
        </div>
        
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
            <div class="text-center group">
                <div class="relative mb-8">
                    <div class="w-24 h-24 mx-auto bg-gradient-to-br from-green-500 to-blue-500 rounded-full flex items-center justify-center text-white text-3xl group-hover:scale-110 transition-transform duration-300 shadow-xl">
                        🍱
                    </div>
                    <div class="absolute -top-2 -right-2 w-8 h-8 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center text-white text-sm font-bold">
                        1
                    </div>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">@LocalizationService.GetString("Home.ChooseRollsTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.ChooseRollsDescription")</p>
            </div>
            
            <div class="text-center group">
                <div class="relative mb-8">
                    <div class="w-24 h-24 mx-auto bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center text-white text-3xl group-hover:scale-110 transition-transform duration-300 shadow-xl">
                        💳
                    </div>
                    <div class="absolute -top-2 -right-2 w-8 h-8 bg-gradient-to-br from-orange-500 to-red-500 rounded-full flex items-center justify-center text-white text-sm font-bold">
                        2
                    </div>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">@LocalizationService.GetString("Home.SecurePaymentTitle")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.SecurePaymentDescription")</p>
            </div>
        
            <div class="text-center group">
                <div class="relative mb-8">
                    <div class="w-24 h-24 mx-auto bg-gradient-to-br from-orange-500 to-red-500 rounded-full flex items-center justify-center text-white text-3xl group-hover:scale-110 transition-transform duration-300 shadow-xl">
                        🚚
                    </div>
                    <div class="absolute -top-2 -right-2 w-8 h-8 bg-gradient-to-br from-green-500 to-blue-500 rounded-full flex items-center justify-center text-white text-sm font-bold">
                        3
                    </div>
                </div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">@LocalizationService.GetString("Home.FastDeliveryTitle2")</h3>
                <p class="text-gray-600 leading-relaxed">@LocalizationService.GetString("Home.TrackOrderDescription")</p>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section with Sakura Pattern -->
<section class="py-20 bg-sakura-layered pattern-overlay-sakura">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="mb-8">
            <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                @LocalizationService.GetString("Home.ReadyToOrder")
                <span class="bg-gradient-to-r from-green-600 via-blue-600 to-purple-600 bg-clip-text text-transparent">
                    @LocalizationService.GetString("Home.OrderQuestion")
                </span>
            </h2>
            <p class="text-xl text-gray-600 mb-8 max-w-2xl mx-auto">
                @LocalizationService.GetString("Home.OrderDescription")
            </p>
        </div>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <button @onclick="@(() => Navigation.NavigateTo("/menu"))" 
                    class="group inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold rounded-2xl text-lg transition-all duration-300 transform hover:scale-105 shadow-xl hover:shadow-2xl">
                <span class="mr-2">🍣</span>
                @LocalizationService.GetString("Home.ViewMenu")
                <svg class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                </svg>
            </button>
            <button @onclick="@(() => Navigation.NavigateTo("/build-roll"))" 
                    class="group inline-flex items-center justify-center px-8 py-4 bg-white/90 backdrop-blur-lg text-gray-700 border-2 border-gray-300 hover:border-green-400 font-semibold rounded-2xl text-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                <span class="mr-2">🎨</span>
                @LocalizationService.GetString("Home.BuildYourOwn")
            </button>
        </div>
    </div>
</section>

@code {
    private List<SushiRoll> signatureRolls = new();
    private bool isLoadingRolls = true;

    protected override async Task OnInitializedAsync()
    {
        await LoadSignatureRolls();
        LocalizationService.OnLanguageChanged += OnLanguageChanged;
    }

    private void OnLanguageChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        LocalizationService.OnLanguageChanged -= OnLanguageChanged;
    }

    private async Task LoadSignatureRolls()
    {
        try
        {
            isLoadingRolls = true;
            var allRolls = await ApiService.GetSushiRollsAsync();
            signatureRolls = allRolls?.Where(r => r.IsSignatureRoll && r.IsAvailable).Take(6).ToList() ?? new();
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading signature rolls from backend");
            signatureRolls = new List<SushiRoll>();
        }
        finally
        {
            isLoadingRolls = false;
            StateHasChanged();
        }
    }

    private void AddToCart(SushiRoll roll)
    {
        try
        {
            CartService.AddItem(roll);
            ToastService.ShowSuccess($"{roll.Name} added to cart!", "Success");
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error adding {RollName} to cart", roll.Name);
            ToastService.ShowError("Failed to add item to cart", "Error");
        }
    }

    private string GetRollImageUrl(string rollName)
    {
        // Map roll names to image URLs
        return rollName.ToLower() switch
        {
            "dragon roll" => "/images/rolls/dragon-roll.jpg",
            "california roll" => "/images/rolls/california-roll.jpg",
            "salmon teriyaki" => "/images/rolls/salmon-teriyaki.jpg",
            "spicy tuna" => "/images/rolls/spicy-tuna.jpg",
            _ => "/images/rolls/default-roll.jpg"
        };
    }
} 